# GitHub Pages

Site URL: **https://oscarovanger.github.io/DS_Chapter/**

Rebuild: `.venv\Scripts\python.exe scripts\prepare_github_pages.py` then commit and push `docs/`.

Enable Pages: repo **Settings → Pages → GitHub Actions** (or deploy from branch `main` / folder `docs`).
